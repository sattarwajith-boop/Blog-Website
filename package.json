{
  "name": "trending-auto-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-posts.mjs",
    "check": "node --check scripts/generate-posts.mjs && node --check scripts/serve-preview.mjs",
    "preview": "node scripts/serve-preview.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
